---
  - name: Initial setup
    hosts: all
    roles:
      - init

  - name: Database Server
    hosts: db_servers
    roles:
      - mysql

  - name: Nginx
    hosts: web_servers
    roles:
      - agama
      - uwsgi
      - nginx
